## 1. Паспорт проверки — Employees

| Поле | Значение |
| --- | --- |
| Проект | pandoraChecklists |
| Функциональность | `employees` (сотрудники) |
| Окружение | `Dev / Test / Staging` |
| Версия API / Commit | _тег/commit_ |
| Base URL | `http://localhost:8081` |
| Документация | Swagger `/swagger/index.html` |
| Роль / способ авторизации | Токенов нет. Внутренняя сеть |
| Проба живости | `GET /ping` → `200`, пустое тело |
| Итог | `Pass / Fail / Blocked / Not Run` |

## 2. Объём проверки — Employees

| Что проверяется | Что не проверяется / ограничения |
| --- | -- |
| Создание сотрудника (`POST /employees`) | |
| Получение списка сотрудников (`GET /employees`) | |
| Получение сотрудника по ID (`GET /employees?employee_id=`) | |
| Получение сотрудников по area_id (`GET /employees?area_id=`) | |
| Получение сотрудников по position_id (`GET /employees?position_id=`) | |
| Получение сотрудников по badge (`GET /employees?employee_badge=`) | |
| Обновление сотрудника (`PATCH /employees/{id}`) | |
| Удаление сотрудника (`DELETE /employees/{id}`) | |
| Валидация обязательных полей (`employee_name`, `employee_surname`, `area_id`, `position_id`, `employee_badge`) | |
| Валидация внешних ключей (`area_id`, `position_id`) | |
| Валидация уникальности поля `employee_badge` | |
| Обработка пустых значений при создании | |
| Обработка ошибок (`400`, `404`, `409`) | |
| Пагинация (`limit`, `offset`) | |

### Инварианты контракта (проверять в каждом кейсе)

- **Источник истины — HTTP-статус.** В JSON нет полей `response_code` / `error_code`.
- **Тело ответа:** успех с данными → `200` + массив объектов; создание → `201` + объект; удаление → `204` без тела.
- **Пагинация:** параметры `limit` (дефолт 0, макс 500) и `offset` (дефолт 0).

## 3. Endpoint-ы — Employees

| ID | Метод | Endpoint | Назначение | Приоритет | Статус |
| --- | --- | --- | --- | --- | --- |
| API-01 | `GET` | `/api/v1/employees` | Получить список сотрудников | `High` | `Pass` |
| API-02 | `GET` | `/api/v1/employees?limit=&offset=` | Получить список с пагинацией | `High` | `Pass` |
| API-03 | `GET` | `/api/v1/employees?employee_id=` | Получить сотрудника по ID | `High` | `Pass` |
| API-04 | `GET` | `/api/v1/employees?area_id=` | Получить сотрудников по зоне | `High` | `Pass` |
| API-05 | `GET` | `/api/v1/employees?position_id=` | Получить сотрудников по позиции | `High` | `Pass` |
| API-06 | `GET` | `/api/v1/employees?employee_badge=` | Получить сотрудников по badge | `High` | `Pass` |
| API-07 | `POST` | `/api/v1/employees` | Создать сотрудника | `High` | `Pass` |
| API-08 | `PATCH` | `/api/v1/employees/{id}` | Обновить сотрудника (частично) | `Medium` | `Pass` |
| API-09 | `DELETE` | `/api/v1/employees/{id}` | Удалить сотрудника | `High` | `Pass` |

## 4. Предусловия и данные — Employees

### Предусловия

- Окружение доступно: `GET /ping` → `200`.
- В БД существует хотя бы один сотрудник (получается динамически через API).
- В БД существуют зоны и позиции для выбора новых значений.
- Для тестов используются реальные данные, полученные из системы.

### Тестовые данные — Employees

| Название | Значение | Где используется | Примечание |
|----------|----------|------------------|------------|
| Employee ID (существующий) | Динамически из API | TC-003, TC-018, TC-019, TC-020, TC-021, TC-022, TC-023, TC-024, TC-025 | Получается через GET /employees |
| Employee Name (существующий) | Динамически из API | TC-018 | Получается из первого сотрудника |
| Employee Surname (существующий) | Динамически из API | TC-022 | Получается из первого сотрудника |
| Employee Patronymic (существующий) | Динамически из API | TC-023 | Получается из первого сотрудника |
| Employee Badge (существующий) | Динамически из API | TC-009, TC-024, TC-026, TC-027 | Получается из первого сотрудника |
| Employee Area IDs (существующие) | Динамически из API | TC-005, TC-019 | Получается из первого сотрудника |
| Employee Position ID (существующий) | Динамически из API | TC-007, TC-020 | Получается из первого сотрудника |
| New Area ID | Динамически из API | TC-011, TC-012, TC-013, TC-014, TC-015, TC-016, TC-019, TC-026, TC-027 | Выбирается из зон, не привязанных к сотруднику |
| New Position ID | Динамически из API | TC-011, TC-012, TC-013, TC-014, TC-015, TC-016, TC-020, TC-026, TC-027 | Выбирается из позиций, не привязанных к сотруднику |
| Employee ID (несуществующий) | `9999999` | TC-004, TC-017 | Не существует в БД |
| Area ID (несуществующий) | `9999999` | TC-006, TC-012, TC-021 | Не существует в БД |
| Position ID (несуществующий) | `9999999` | TC-008, TC-013, TC-025 | Не существует в БД |
| Badge (несуществующий) | `88888888` | TC-010 | Не существует в БД |
| Name (для создания) | `TestName` | TC-011, TC-012, TC-013, TC-014, TC-015, TC-016, TC-026, TC-027 | Название для создания |
| Surname (для создания) | `TestSurname` | TC-011, TC-012, TC-013, TC-014, TC-015, TC-016, TC-022, TC-026, TC-027 | Фамилия для создания/обновления |
| Patronymic (для создания) | `TestPatronymic` | TC-011, TC-012, TC-013, TC-014, TC-015, TC-016, TC-023, TC-026, TC-027 | Отчество для создания/обновления |
| Badge (для создания) | `12345678` | TC-011, TC-012, TC-013, TC-014, TC-015, TC-016, TC-024, TC-027 | Badge для создания/обновления |
| Limit | `10` | TC-002 | Лимит для пагинации |
| Offset | `1` | TC-002 | Смещение для пагинации |

## 5. Тест-кейсы — Employees

| ID | Endpoint | Сценарий | Тип | Шаги / запрос | Ожидаемый результат | Фактический результат | Статус | Доказательства |
|-----|----------|----------|-----|---------------|----------------------|-----------------------|--------|----------------|
| TC-001 | `GET /employees` | Получение списка сотрудников | `Positive` | GET без параметров | `200` + массив объектов; структура содержит `employee_id`, `employee_name`, `employee_surname`, `employee_patronymic`, `area_id`, `position_id`, `employee_badge` | Совпадает с ожидаемым | `Pass` | |
| TC-002 | `GET /employees?limit=&offset=` | Получение списка с пагинацией | `Positive` | GET с параметрами `limit=10&offset=1` | `200` + массив объектов; количество ≤ `limit`; сотрудник с ID из test_data исключен | Совпадает с ожидаемым | `Pass` | |
| TC-003 | `GET /employees?employee_id=` | Получение сотрудника по ID | `Positive` | GET с `employee_id` из test_data | `200` + массив с одним объектом; данные соответствуют | Совпадает с ожидаемым | `Pass` | |
| TC-004 | `GET /employees?employee_id=` | Получение по несуществующему ID | `Negative` | GET с `employee_id: 9999999` | `200` + пустой массив `[]` | Совпадает с ожидаемым | `Pass` | |
| TC-005 | `GET /employees?area_id=` | Получение сотрудников по area_id | `Positive` | GET с `area_id` из test_data | `200` + массив объектов; у всех `area_id` содержит переданное значение | Совпадает с ожидаемым | `Pass` | |
| TC-006 | `GET /employees?area_id=` | Получение по несуществующему area_id | `Negative` | GET с `area_id: 9999999` | `200` + пустой массив `[]` | Совпадает с ожидаемым | `Pass` | |
| TC-007 | `GET /employees?position_id=` | Получение сотрудников по position_id | `Positive` | GET с `position_id` из test_data | `200` + массив объектов; у всех `position_id` равен переданному | Совпадает с ожидаемым | `Pass` | |
| TC-008 | `GET /employees?position_id=` | Получение по несуществующему position_id | `Negative` | GET с `position_id: 9999999` | `200` + пустой массив `[]` | Совпадает с ожидаемым | `Pass` | |
| TC-009 | `GET /employees?employee_badge=` | Получение сотрудников по badge | `Positive` | GET с `employee_badge` из test_data | `200` + массив объектов; у всех `employee_badge` равен переданному | Совпадает с ожидаемым | `Pass` | |
| TC-010 | `GET /employees?employee_badge=` | Получение по несуществующему badge | `Negative` | GET с `employee_badge: 88888888` | `200` + пустой массив `[]` | Совпадает с ожидаемым | `Pass` | |
| TC-011 | `POST /employees` | Создание сотрудника с валидными данными | `Positive` | POST с телом: <br/> `{"employee_name": "TestName", "employee_surname": "TestSurname", "employee_patronymic": "TestPatronymic", "area_id": [new_area_id], "position_id": new_position_id, "employee_badge": 12345678}` | `201` + созданный объект; все поля соответствуют | Совпадает с ожидаемым | `Pass` | |
| TC-012 | `POST /employees` | Создание с несуществующим area_id | `Negative` | POST с `area_id: [9999999]` | Должен быть `409` + ошибка валидации | Совпадает с ожидаемым | `Pass` | |
| TC-013 | `POST /employees` | Создание с несуществующим position_id | `Negative` | POST с `position_id: 9999999` | Должен быть `409` + ошибка валидации | Совпадает с ожидаемым | `Pass` | |
| TC-014 | `POST /employees` | Создание с пустым `employee_name` | `Negative` | POST с `employee_name: ""` | Должен быть `400` + ошибка валидации | Совпадает с ожидаемым | `Pass` | |
| TC-015 | `POST /employees` | Создание с пустым `employee_surname` | `Negative` | POST с `employee_surname: ""` | Должен быть `400` + ошибка валидации | **БАГ:** API возвращает `201` и создает сотрудника с пустой фамилией вместо `400` | `Fail` | |
| TC-016 | `DELETE /employees/{id}` | Удаление существующего сотрудника | `Positive` | 1. Создать сотрудника <br/> 2. DELETE по ID <br/> 3. GET по ID для проверки | `204 No Content`; последующий GET возвращает `200` + пустой массив | Совпадает с ожидаемым | `Pass` | |
| TC-017 | `DELETE /employees/{id}` | Удаление по несуществующему ID | `Negative` | DELETE `id: 9999999` | `404` + ошибка `employee not found` | Совпадает с ожидаемым | `Pass` | |
| TC-018 | `PATCH /employees/{id}` | Обновление имени сотрудника | `Positive` | 1. Взять существующего сотрудника <br/> 2. PATCH с новым именем <br/> 3. Восстановить исходное имя | `200` + обновлённый объект; имя изменено | Совпадает с ожидаемым | `Pass` | |
| TC-019 | `PATCH /employees/{id}` | Добавление новой зоны сотруднику | `Positive` | 1. Взять существующего сотрудника <br/> 2. PATCH с добавлением новой зоны в `area_id` <br/> 3. Восстановить исходные зоны | `200` + обновлённый объект; зона добавлена в список | Совпадает с ожидаемым | `Pass` | |
| TC-020 | `PATCH /employees/{id}` | Изменение позиции сотрудника | `Positive` | 1. Взять существующего сотрудника <br/> 2. PATCH с новой позицией <br/> 3. Восстановить исходную позицию | `200` + обновлённый объект; позиция изменена | Совпадает с ожидаемым | `Pass` | |
| TC-021 | `PATCH /employees/{id}` | Обновление с несуществующей зоной | `Negative` | PATCH с `area_id: [9999999]` | Должен быть `409` + ошибка валидации | Совпадает с ожидаемым | `Pass` | |
| TC-022 | `PATCH /employees/{id}` | Обновление фамилии сотрудника | `Positive` | 1. Взять существующего сотрудника <br/> 2. PATCH с новой фамилией <br/> 3. Восстановить исходную фамилию | `200` + обновлённый объект; фамилия изменена | **БАГ:** PATCH не обновляет фамилию (ошибка в тесте) | `Fail` | |
| TC-023 | `PATCH /employees/{id}` | Обновление отчества сотрудника | `Positive` | 1. Взять существующего сотрудника <br/> 2. PATCH с новым отчеством <br/> 3. Восстановить исходное отчество | `200` + обновлённый объект; отчество изменено | **БАГ:** PATCH не обновляет отчество (ошибка в тесте) | `Fail` | |
| TC-024 | `PATCH /employees/{id}` | Обновление badge сотрудника | `Positive` | 1. Взять существующего сотрудника <br/> 2. PATCH с новым badge <br/> 3. Восстановить исходный badge | `200` + обновлённый объект; badge изменен | **БАГ:** PATCH не обновляет badge (ошибка в тесте) | `Fail` | |
| TC-025 | `PATCH /employees/{id}` | Обновление с несуществующей позицией | `Negative` | PATCH с `position_id: 9999999` | Должен быть `409` + ошибка валидации | Совпадает с ожидаемым | `Pass` | |
| TC-026 | `POST /employees` | Создание сотрудника с дублирующимся badge | `Negative` | POST с `employee_badge` из существующего сотрудника | Должен быть `409` + ошибка `resource already exists` | **БАГ:** API возвращает `201` и создает сотрудника с дублирующимся badge вместо `409` | `Fail` | |
| TC-027 | `PATCH /employees/{id}` | Обновление badge на дублирующийся | `Negative` | 1. Создать сотрудника с уникальным badge <br/> 2. PATCH с badge существующего сотрудника | Должен быть `409` + ошибка `resource already exists` | **БАГ:** API возвращает `400` вместо `409` | `Fail` | |

## 6. Дефекты — Employees

| ID | Краткое описание | Severity | Endpoint / метод | Шаги воспроизведения | Ожидаемо | Фактически | X-Request-Id | Статус         |
|-----|------------------|----------|------------------|----------------------|----------|------------|--------------|----------------|
| BUG-001 | Отсутствует валидация пустого `employee_surname` при создании сотрудника | `Major` | `POST /employees` | 1. Отправить запрос на создание сотрудника с `employee_surname: ""` | Должна быть ошибка `400` с описанием проблемы валидации | API возвращает `201` и создает сотрудника с пустой фамилией | | `Closed`  |
| BUG-002 | PATCH не обновляет поле `employee_surname` | `Critical` | `PATCH /employees/{id}` | 1. Взять существующего сотрудника <br/> 2. Отправить PATCH с новым значением `employee_surname` | Поле `employee_surname` должно обновиться на новое значение | Поле `employee_surname` не обновляется | | `Closed`  |
| BUG-003 | PATCH не обновляет поле `employee_patronymic` | `Critical` | `PATCH /employees/{id}` | 1. Взять существующего сотрудника <br/> 2. Отправить PATCH с новым значением `employee_patronymic` | Поле `employee_patronymic` должно обновиться на новое значение | Поле `employee_patronymic` не обновляется | | `Closed`  |
| BUG-004 | PATCH не обновляет поле `employee_badge` | `Critical` | `PATCH /employees/{id}` | 1. Взять существующего сотрудника <br/> 2. Отправить PATCH с новым значением `employee_badge` | Поле `employee_badge` должно обновиться на новое значение | Поле `employee_badge` не обновляется | | `Closed`  |
| BUG-005 | Отсутствует валидация уникальности `employee_badge` при создании сотрудника | `Critical` | `POST /employees` | 1. Создать сотрудника с уникальным badge <br/> 2. Создать другого сотрудника с таким же badge | Должна быть ошибка `409` с описанием `resource already exists` | API возвращает `201` и создает сотрудника с дублирующимся badge | | `Closed`  |
| BUG-006 | Некорректный статус ответа при попытке обновить badge на дублирующийся | `Major` | `PATCH /employees/{id}` | 1. Создать сотрудника с уникальным badge <br/> 2. Создать другого сотрудника <br/> 3. Обновить второго сотрудника, указав badge первого | Должна быть ошибка `409` с описанием `resource already exists` | API возвращает `400` вместо `409` | | `Closed`  |

## 7. Итог — Employees

| Статус | Количество |
|--------|------------|
| Pass | 22         |
| Fail | 5          |
