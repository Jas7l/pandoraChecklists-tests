## 1. Паспорт проверки — Machines

| Поле | Значение |
| --- | --- |
| Проект | pandoraChecklists |
| Функциональность | `machines` (станки/оборудование) |
| Окружение | `Dev / Test / Staging` |
| Версия API / Commit | _тег/commit_ |
| Base URL | `http://localhost:8081` |
| Документация | Swagger `/swagger/index.html` |
| Роль / способ авторизации | Токенов нет. Внутренняя сеть |
| Проба живости | `GET /ping` → `200`, пустое тело |
| Итог | `Pass / Fail / Blocked / Not Run` |

## 2. Объём проверки — Machines

| Что проверяется | Что не проверяется / ограничения |
| --- | -- |
| Создание станка (`POST /machines`) | |
| Получение списка станков (`GET /machines`) | |
| Получение станка по ID (`GET /machines?machine_id=`) | |
| Получение станков по area_id (`GET /machines?area_id=`) | |
| Получение станков по status (`GET /machines?status=`) | |
| Обновление станка (`PATCH /machines/{id}`) | |
| Удаление станка (`DELETE /machines/{id}`) | |
| Валидация обязательных полей (`machine_name`, `area_id`) | |
| Валидация внешних ключей (`area_id`) | |
| Валидация статуса (`not_checked`, `accident`, `ok`) | |
| Обновление статуса и `status_updated_at` | |
| Обработка ошибок (`400`, `404`, `409`) | |
| Пагинация (`limit`, `offset`) | |

### Инварианты контракта (проверять в каждом кейсе)

- **Источник истины — HTTP-статус.** В JSON нет полей `response_code` / `error_code`.
- **Тело ответа:** успех с данными → `200` + массив объектов; создание → `201` + объект; удаление → `204` без тела.
- **Пагинация:** параметры `limit` (дефолт 0, макс 500) и `offset` (дефолт 0).

## 3. Endpoint-ы — Machines

| ID | Метод | Endpoint | Назначение | Приоритет | Статус |
| --- | --- | --- | --- | --- | --- |
| API-01 | `GET` | `/api/v1/machines` | Получить список станков | `High` | `Pass` |
| API-02 | `GET` | `/api/v1/machines?limit=&offset=` | Получить список с пагинацией | `High` | `Pass` |
| API-03 | `GET` | `/api/v1/machines?machine_id=` | Получить станок по ID | `High` | `Pass` |
| API-04 | `GET` | `/api/v1/machines?area_id=` | Получить станки по зоне | `High` | `Pass` |
| API-05 | `GET` | `/api/v1/machines?status=` | Получить станки по статусу | `High` | `Pass` |
| API-06 | `POST` | `/api/v1/machines` | Создать станок | `High` | `Pass` |
| API-07 | `PATCH` | `/api/v1/machines/{id}` | Обновить станок (частично) | `Medium` | `Pass` |
| API-08 | `DELETE` | `/api/v1/machines/{id}` | Удалить станок | `High` | `Pass` |

## 4. Предусловия и данные — Machines

### Предусловия

- Окружение доступно: `GET /ping` → `200`.
- В БД существует хотя бы один станок (получается динамически через API).
- В БД существует хотя бы две зоны (для выбора новой зоны при тестировании).
- Для тестов используются реальные данные, полученные из системы.

### Тестовые данные — Machines

| Название | Значение | Где используется | Примечание |
|----------|----------|------------------|------------|
| Machine ID (существующий) | Динамически из API | TC-003, TC-013, TC-014, TC-015, TC-016, TC-017 | Получается через GET /machines |
| Machine Name (существующий) | Динамически из API | TC-002, TC-013 | Получается из первого станка |
| Area ID (существующий) | Динамически из API | TC-005, TC-009, TC-014 | Получается из первого станка |
| Status (существующий) | Динамически из API | TC-003, TC-015 | Получается из первого станка |
| Status Updated At (существующий) | Динамически из API | TC-015 | Получается из первого станка |
| New Area ID | Динамически из API | TC-014 | Выбирается из зон, отличных от текущей |
| Machine ID (несуществующий) | `9999999` | TC-004, TC-012, TC-017 | Не существует в БД |
| Area ID (несуществующий) | `9999999` | TC-006, TC-010, TC-017 | Не существует в БД |
| Name (для создания) | `test machine` | TC-009, TC-011, TC-013 | Название для создания/обновления |
| Статусы (валидные) | `not_checked`, `accident`, `ok` | TC-007, TC-015 | Валидные статусы станка |
| Статус (невалидный) | `invalid_status` | TC-008, TC-016 | Невалидный статус |
| Limit | `10` | TC-002 | Лимит для пагинации |
| Offset | `1` | TC-002 | Смещение для пагинации |

## 5. Тест-кейсы — Machines

| ID | Endpoint | Сценарий | Тип | Шаги / запрос | Ожидаемый результат | Фактический результат | Статус | Доказательства |
|-----|----------|----------|-----|---------------|----------------------|-----------------------|--------|----------------|
| TC-001 | `GET /machines` | Получение списка станков | `Positive` | GET без параметров | `200` + массив объектов; структура содержит `machine_id`, `machine_name`, `area_id`, `status`, `status_updated_at` | Совпадает с ожидаемым | `Pass` | |
| TC-002 | `GET /machines?limit=&offset=` | Получение списка с пагинацией | `Positive` | GET с параметрами `limit=10&offset=1` | `200` + массив объектов; количество ≤ `limit`; станок с ID из test_data исключен | Совпадает с ожидаемым | `Pass` | |
| TC-003 | `GET /machines?machine_id=` | Получение станка по ID | `Positive` | GET с `machine_id` из test_data | `200` + массив с одним объектом; данные соответствуют | Совпадает с ожидаемым | `Pass` | |
| TC-004 | `GET /machines?machine_id=` | Получение по несуществующему ID | `Negative` | GET с `machine_id: 9999999` | `200` + пустой массив `[]` | Совпадает с ожидаемым | `Pass` | |
| TC-005 | `GET /machines?area_id=` | Получение станков по area_id | `Positive` | GET с `area_id` из test_data | `200` + массив объектов; у всех `area_id` равен переданному | Совпадает с ожидаемым | `Pass` | |
| TC-006 | `GET /machines?area_id=` | Получение по несуществующему area_id | `Negative` | GET с `area_id: 9999999` | `200` + пустой массив `[]` | Совпадает с ожидаемым | `Pass` | |
| TC-007 | `GET /machines?status=` | Получение станков по статусу | `Positive` | **Параметризованный тест:** `not_checked`, `accident`, `ok` | `200` + массив объектов; у всех `status` равен переданному | Совпадает с ожидаемым | `Pass` | |
| TC-008 | `GET /machines?status=` | Получение по невалидному статусу | `Negative` | GET с `status: invalid_status` | Должен быть `400` + ошибка валидации | Совпадает с ожидаемым | `Pass` | |
| TC-009 | `POST /machines` | Создание станка с валидными данными | `Positive` | POST с телом: <br/> `{"machine_name": "test machine", "area_id": <area_id>}` | `201` + созданный объект; поля соответствуют; статус по умолчанию `not_checked` | Совпадает с ожидаемым | `Pass` | |
| TC-010 | `POST /machines` | Создание с несуществующей зоной | `Negative` | POST с `area_id: 9999999` | Должен быть `409` + ошибка валидации | Совпадает с ожидаемым | `Pass` | |
| TC-011 | `DELETE /machines/{id}` | Удаление существующего станка | `Positive` | 1. Создать станок <br/> 2. DELETE по ID | `204 No Content` | Совпадает с ожидаемым | `Pass` | |
| TC-012 | `DELETE /machines/{id}` | Удаление по несуществующему ID | `Negative` | DELETE `id: 9999999` | `404` + ошибка `machine not found` | Совпадает с ожидаемым | `Pass` | |
| TC-013 | `PATCH /machines/{id}` | Обновление названия станка | `Positive` | 1. Взять существующий станок <br/> 2. PATCH с новым именем <br/> 3. Восстановить исходное имя | `200` + обновлённый объект; `machine_name` изменено | Совпадает с ожидаемым | `Pass` | |
| TC-014 | `PATCH /machines/{id}` | Обновление зоны станка | `Positive` | 1. Взять существующий станок <br/> 2. PATCH с новой зоной <br/> 3. Восстановить исходную зону | `200` + обновлённый объект; `area_id` изменен | Совпадает с ожидаемым | `Pass` | |
| TC-015 | `PATCH /machines/{id}` | Обновление статуса станка | `Positive` | **Параметризованный тест:** `not_checked`, `accident`, `ok` <br/> 1. Взять существующий станок <br/> 2. PATCH с новым статусом <br/> 3. Проверить обновление `status_updated_at` <br/> 4. Восстановить исходный статус | `200` + обновлённый объект; `status` изменен; `status_updated_at` обновлен | Совпадает с ожидаемым | `Pass` | |
| TC-016 | `PATCH /machines/{id}` | Обновление с невалидным статусом | `Negative` | PATCH с `status: invalid_status` | Должен быть `400` + ошибка валидации | Совпадает с ожидаемым | `Pass` | |
| TC-017 | `PATCH /machines/{id}` | Обновление с несуществующей зоной | `Negative` | PATCH с `area_id: 9999999` | Должен быть `409` + ошибка валидации | Совпадает с ожидаемым | `Pass` | |

## 6. Дефекты — Machines

| ID | Краткое описание | Severity | Endpoint / метод | Шаги воспроизведения | Ожидаемо | Фактически | X-Request-Id | Статус |
|-----|------------------|----------|------------------|----------------------|----------|------------|--------------|--------|
| BUG-001 | Нет выявленных дефектов | - | - | - | - | - | - | - |

## 7. Итог — Machines

| Статус | Количество |
|--------|------------|
| Pass | 17 |
| Fail | 0 |
