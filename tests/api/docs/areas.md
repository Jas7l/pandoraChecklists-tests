## 1. Паспорт проверки — Areas

| Поле | Значение |
| --- | --- |
| Проект | pandoraChecklists |
| Функциональность | `areas` (зоны/участки) |
| Окружение | `Dev / Test / Staging` |
| Версия API / Commit | _тег/commit_ |
| Base URL | `http://localhost:8081` |
| Документация | Swagger `/swagger/index.html` |
| Роль / способ авторизации | Токенов нет. Внутренняя сеть |
| Проба живости | `GET /ping` → `200`, пустое тело |
| Итог | `Pass / Fail / Blocked / Not Run` |

## 2. Объём проверки — Areas

| Что проверяется | Что не проверяется / ограничения |
| --- | -- |
| Создание зоны (`POST /areas`) | |
| Получение списка зон (`GET /areas`) | |
| Получение зоны по ID (`GET /areas?area_id=`) | |
| Обновление зоны (`PATCH /areas/{id}`) | |
| Удаление зоны (`DELETE /areas/{id}`) | |
| Валидация обязательных полей (`area_name`) | |
| Валидация уникальности поля `area_name` | |
| Проверка каскадных ограничений при удалении | |
| Обработка пустых значений при создании | |
| Обработка ошибок (`400`, `404`, `409`) | |
| Пагинация (`limit`, `offset`) | |

### Инварианты контракта (проверять в каждом кейсе)

- **Источник истины — HTTP-статус.** В JSON нет полей `response_code` / `error_code`.
- **Тело ответа:** успех с данными → `200` + массив объектов; создание → `201` + объект; удаление → `204` без тела.
- **Пагинация:** параметры `limit` (дефолт 0, макс 500) и `offset` (дефолт 0).

## 3. Endpoint-ы — Areas

| ID | Метод | Endpoint | Назначение | Приоритет | Статус |
| --- | --- | --- | --- | --- | --- |
| API-01 | `GET` | `/api/v1/areas` | Получить список зон | `High` | `Pass` |
| API-02 | `GET` | `/api/v1/areas?limit=&offset=` | Получить список с пагинацией | `High` | `Pass` |
| API-03 | `GET` | `/api/v1/areas?area_id=` | Получить зону по ID | `High` | `Pass` |
| API-04 | `POST` | `/api/v1/areas` | Создать зону | `High` | `Pass` |
| API-05 | `PATCH` | `/api/v1/areas/{id}` | Обновить зону (частично) | `Medium` | `Pass` |
| API-06 | `DELETE` | `/api/v1/areas/{id}` | Удалить зону | `High` | `Pass` |

## 4. Предусловия и данные — Areas

### Предусловия

- Окружение доступно: `GET /ping` → `200`.
- В БД существует хотя бы одна зона (получается динамически через API).
- Для тестов используются реальные данные, полученные из системы.

### Тестовые данные — Areas

| Название | Значение | Где используется | Примечание |
|----------|----------|------------------|------------|
| Area ID (существующий) | Динамически из API | TC-003, TC-010 | Получается через GET /areas |
| Area Name (существующий) | Динамически из API | TC-006, TC-011 | Получается из первой зоны |
| Area ID (несуществующий) | `9999999` | TC-004, TC-009 | Не существует в БД |
| Name (для создания) | `test area` | TC-005, TC-008, TC-011 | Название для создания/обновления |
| Machine Name (для создания) | `test machine` | TC-012 | Название станка для теста |
| Limit | `10` | TC-002 | Лимит для пагинации |
| Offset | `1` | TC-002 | Смещение для пагинации |

## 5. Тест-кейсы — Areas

| ID | Endpoint | Сценарий | Тип | Шаги / запрос | Ожидаемый результат | Фактический результат | Статус | Доказательства |
|-----|----------|----------|-----|---------------|----------------------|-----------------------|--------|----------------|
| TC-001 | `GET /areas` | Получение списка зон | `Positive` | GET без параметров | `200` + массив объектов; структура объектов содержит поля `area_id` и `area_name` | Совпадает с ожидаемым | `Pass` | |
| TC-002 | `GET /areas?limit=&offset=` | Получение списка зон с пагинацией | `Positive` | GET с параметрами `limit=10&offset=1` | `200` + массив объектов; количество объектов ≤ `limit`; зона с ID из test_data исключена из результатов (offset=1) | Совпадает с ожидаемым | `Pass` | |
| TC-003 | `GET /areas?area_id=` | Получение зоны по существующему ID | `Positive` | GET с `area_id` из test_data | `200` + массив с одним объектом; `area_id` и `area_name` соответствуют данным из системы | Совпадает с ожидаемым | `Pass` | |
| TC-004 | `GET /areas?area_id=` | Получение зоны по несуществующему ID | `Negative` | GET с `area_id: 9999999` | `200` + пустой массив `[]` | Совпадает с ожидаемым | `Pass` | |
| TC-005 | `POST /areas` | Создание зоны с валидными данными | `Positive` | POST с телом: <br/> `{"area_name": "test area"}` | `201` + созданный объект; поле `area_name` соответствует переданному; есть `area_id` | Совпадает с ожидаемым | `Pass` | |
| TC-006 | `POST /areas` | Создание зоны с дублирующимся `area_name` | `Negative` | POST с телом: <br/> `{"area_name": "<имя_из_существующей_зоны>"}` | Должен быть `409` + ошибка `resource already exists` | **БАГ:** API возвращает `201` и создает зону с дублирующимся именем вместо `409` | `Fail` | |
| TC-007 | `POST /areas` | Создание зоны с пустым `area_name` | `Negative` | POST с телом: <br/> `{"area_name": ""}` | Должен быть `400` + ошибка валидации | Совпадает с ожидаемым | `Pass` | |
| TC-008 | `DELETE /areas/{id}` | Удаление существующей зоны | `Positive` | 1. Создать зону <br/> 2. DELETE по ID созданной зоны | `204 No Content` | Совпадает с ожидаемым | `Pass` | |
| TC-009 | `DELETE /areas/{id}` | Удаление по несуществующему ID | `Negative` | DELETE `id: 9999999` | `404` + ошибка `area not found` | Совпадает с ожидаемым | `Pass` | |
| TC-010 | `PATCH /areas/{id}` | Обновление зоны | `Positive` | 1. Взять существующую зону <br/> 2. PATCH с новым именем `test area` <br/> 3. Проверить что имя обновилось <br/> 4. Восстановить исходное имя | `200` + обновлённый объект; поле `area_name` обновлено на новое значение | Совпадает с ожидаемым | `Pass` | |
| TC-011 | `PATCH /areas/{id}` | Обновление с дублирующимся `area_name` | `Negative` | 1. Создать зону <br/> 2. PATCH с именем существующей зоны | Должен быть `409` + ошибка `resource already exists` | **БАГ:** API возвращает `200` и обновляет зону с дублирующимся именем вместо `409` | `Fail` | |
| TC-012 | `DELETE /areas/{id}` | Удаление зоны, привязанной к станку | `Positive` | 1. Создать зону <br/> 2. Создать станок с этой зоной <br/> 3. Удалить зону <br/> 4. Проверить что у станка area_id = None | `204 No Content`; поле `area_id` у станка становится `None` | Совпадает с ожидаемым | `Pass` | |

## 6. Дефекты — Areas

| ID | Краткое описание | Severity | Endpoint / метод | Шаги воспроизведения | Ожидаемо | Фактически | X-Request-Id | Статус |
|-----|------------------|----------|------------------|----------------------|----------|------------|--------------|--------|
| BUG-001 | Отсутствует валидация уникальности `area_name` при создании зоны | `Critical` | `POST /areas` | 1. Создать зону с уникальным именем <br/> 2. Создать другую зону с таким же именем | Должна быть ошибка `409` с описанием `resource already exists` | API возвращает `201` и создает зону с дублирующимся именем | | `Open` |
| BUG-002 | Отсутствует валидация уникальности `area_name` при обновлении зоны | `Major` | `PATCH /areas/{id}` | 1. Создать зону <br/> 2. Создать другую зону <br/> 3. Обновить вторую зону, указав имя первой | Должна быть ошибка `409` с описанием `resource already exists` | API возвращает `200` и обновляет зону с дублирующимся именем | | `Open` |

## 7. Итог — Areas

| Статус | Количество |
|--------|------------|
| Pass | 10 |
| Fail | 2 |
