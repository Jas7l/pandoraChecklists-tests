## 1. Паспорт проверки — Positions

| Поле | Значение |
| --- | --- |
| Проект | pandoraChecklists |
| Функциональность | `positions` (позиции/должности) |
| Окружение | `Dev / Test / Staging` |
| Версия API / Commit | _тег/commit_ |
| Base URL | `http://localhost:8081` |
| Документация | Swagger `/swagger/index.html` |
| Роль / способ авторизации | Токенов нет. Внутренняя сеть |
| Проба живости | `GET /ping` → `200`, пустое тело |
| Итог | `Pass / Fail / Blocked / Not Run` |

## 2. Объём проверки — Positions

| Что проверяется | Что не проверяется / ограничения |
| --- | -- |
| Создание позиции (`POST /positions`) | |
| Получение списка позиций (`GET /positions`) | |
| Получение позиции по ID (`GET /positions?position_id=`) | |
| Обновление позиции (`PATCH /positions/{id}`) | |
| Мягкое удаление позиции (`DELETE /positions/{id}`) | |
| Валидация обязательных полей (`position_name`) | |
| Валидация уникальности поля `position_name` | |
| Обработка пустых значений при создании | |
| Обработка ошибок (`400`, `404`, `409`) | |
| Пагинация (`limit`, `offset`) | |

### Инварианты контракта (проверять в каждом кейсе)

- **Источник истины — HTTP-статус.** В JSON нет полей `response_code` / `error_code`.
- **Тело ответа:** успех с данными → `200` + массив объектов; создание → `201` + объект; удаление → `204` без тела.
- **Пагинация:** параметры `limit` (дефолт 0, макс 500) и `offset` (дефолт 0).

## 3. Endpoint-ы — Positions

| ID | Метод | Endpoint | Назначение                  | Приоритет | Статус |
| --- | --- | --- |-----------------------------| --- | --- |
| API-01 | `GET` | `/api/v1/positions` | Получить список позиций     | `High` | `Pass` |
| API-02 | `GET` | `/api/v1/positions?limit=&offset=` | Получить список с пагинацией | `High` | `Pass` |
| API-03 | `GET` | `/api/v1/positions?position_id=` | Получить позицию по ID      | `High` | `Pass` |
| API-04 | `POST` | `/api/v1/positions` | Создать позицию             | `High` | `Pass` |
| API-05 | `PATCH` | `/api/v1/positions/{id}` | Обновить позицию (частично) | `Medium` | `Pass` |
| API-06 | `DELETE` | `/api/v1/positions/{id}` | Удалить позицию             | `High` | `Pass` |

## 4. Предусловия и данные — Positions

### Предусловия

- Окружение доступно: `GET /ping` → `200`.
- В БД существует хотя бы одна позиция (получается динамически через API).
- Для тестов используется реальная позиция, полученная из системы.

### Тестовые данные — Positions

| Название | Значение | Где используется | Примечание |
|----------|----------|------------------|------------|
| Position ID (существующий) | Динамически из API | TC-003, TC-010 | Получается через GET /positions |
| Position Name (существующий) | Динамически из API | TC-006, TC-011 | Получается из первой позиции |
| Position ID (несуществующий) | `9999999` | TC-004, TC-009 | Не существует в БД |
| Name (для создания) | `test position` | TC-005, TC-008, TC-011 | Название для создания/обновления |
| Limit | `10` | TC-002 | Лимит для пагинации |
| Offset | `1` | TC-002 | Смещение для пагинации |

## 5. Тест-кейсы — Positions

| ID | Endpoint | Сценарий | Тип | Шаги / запрос | Ожидаемый результат | Фактический результат | Статус | Доказательства |
|-----|----------|----------|-----|---------------|----------------------|-----------------------|--------|----------------|
| TC-001 | `GET /positions` | Получение списка позиций | `Positive` | GET без параметров | `200` + массив объектов; структура объектов содержит поля `position_id` и `position_name` | Совпадает с ожидаемым | `Pass` | |
| TC-002 | `GET /positions?limit=&offset=` | Получение списка позиций с пагинацией | `Positive` | GET с параметрами `limit=10&offset=1` | `200` + массив объектов; количество объектов ≤ `limit`; позиция с ID из test_data исключена из результатов (offset=1) | Совпадает с ожидаемым | `Pass` | |
| TC-003 | `GET /positions?position_id=` | Получение позиции по существующему ID | `Positive` | GET с `position_id` из test_data | `200` + массив с одним объектом; `position_id` и `position_name` соответствуют данным из системы | Совпадает с ожидаемым | `Pass` | |
| TC-004 | `GET /positions?position_id=` | Получение позиции по несуществующему ID | `Negative` | GET с `position_id: 9999999` | `200` + пустой массив `[]` | Совпадает с ожидаемым | `Pass` | |
| TC-005 | `POST /positions` | Создание позиции с валидными данными | `Positive` | POST с телом: <br/> `{"position_name": "test position"}` | `201` + созданный объект; поле `position_name` соответствует переданному; есть `position_id` | Совпадает с ожидаемым | `Pass` | |
| TC-006 | `POST /positions` | Создание позиции с дублирующимся `position_name` | `Negative` | POST с телом: <br/> `{"position_name": "<имя_из_существующей_позиции>"}` | Должен быть `409` + ошибка `resource already exists` | **БАГ:** API возвращает `204` (No Content) вместо `409` | `Fail` | |
| TC-007 | `POST /positions` | Создание позиции с пустым `position_name` | `Negative` | POST с телом: <br/> `{"position_name": ""}` | Должен быть `400` + ошибка валидации | **Не тестируется в автоматических тестах** | `Not Run` | |
| TC-008 | `DELETE /positions/{id}` | Мягкое удаление существующей позиции | `Positive` | 1. Создать позицию <br/> 2. DELETE по ID созданной позиции | `204 No Content` | Совпадает с ожидаемым | `Pass` | |
| TC-009 | `DELETE /positions/{id}` | Удаление по несуществующему ID | `Negative` | DELETE `id: 9999999` | `404` + ошибка `position not found` | Совпадает с ожидаемым | `Pass` | |
| TC-010 | `PATCH /positions/{id}` | Обновление позиции | `Positive` | 1. Взять существующую позицию <br/> 2. PATCH с новым именем `test position` <br/> 3. Проверить что имя обновилось <br/> 4. Восстановить исходное имя | `200` + обновлённый объект; поле `position_name` обновлено на новое значение | Совпадает с ожидаемым | `Pass` | |
| TC-011 | `PATCH /positions/{id}` | Обновление с дублирующимся `position_name` | `Negative` | 1. Создать позицию <br/> 2. PATCH с именем существующей позиции | Должен быть `409` + ошибка `resource already exists` | **БАГ:** API возвращает `200` и обновляет позицию с дублирующимся именем вместо `409` | `Fail` | |

## 6. Дефекты — Positions

| ID | Краткое описание | Severity | Endpoint / метод | Шаги воспроизведения | Ожидаемо | Фактически | X-Request-Id | Статус |
|-----|------------------|----------|------------------|----------------------|----------|------------|--------------|--------|
| BUG-001 | Отсутствует валидация уникальности `position_name` при создании позиции (некорректный статус ответа) | `Critical` | `POST /positions` | 1. Создать позицию с уникальным именем <br/> 2. Создать другую позицию с таким же именем | Должна быть ошибка `409` с описанием `resource already exists` | API возвращает `204` (No Content) вместо `409` | | `Open` |
| BUG-002 | Отсутствует валидация уникальности `position_name` при обновлении позиции | `Major` | `PATCH /positions/{id}` | 1. Создать позицию <br/> 2. Создать другую позицию <br/> 3. Обновить вторую позицию, указав имя первой | Должна быть ошибка `409` с описанием `resource already exists` | API возвращает `200` и обновляет позицию с дублирующимся именем | | `Open` |

## 7. Итог — Positions

| Статус | Количество |
|--------|------------|
| Pass | 9 |
| Fail | 2 |
