## 1. Паспорт проверки — Checklists

| Поле | Значение |
| --- | --- |
| Проект | pandoraChecklists |
| Функциональность | `checklists` (чек-листы) |
| Окружение | `Dev / Test / Staging` |
| Версия API / Commit | _тег/commit_ |
| Base URL | `http://localhost:8081` |
| Документация | Swagger `/swagger/index.html` |
| Роль / способ авторизации | Токенов нет. Внутренняя сеть |
| Проба живости | `GET /ping` → `200`, пустое тело |
| Итог | `Pass / Fail / Blocked / Not Run` |

## 2. Объём проверки — Checklists

| Что проверяется | Что не проверяется / ограничения |
| --- | -- |
| Создание чек-листа (`POST /checklists`) | |
| Получение списка чек-листов (`GET /checklists`) | |
| Получение чек-листа по ID (`GET /checklists?checklist_id=`) | |
| Получение чек-листов по machine_id (`GET /checklists?machine_id=`) | |
| Получение чек-листов по status (`GET /checklists?status=`) | |
| Обновление чек-листа (`PATCH /checklists/{id}`) | |
| Удаление чек-листа (`DELETE /checklists/{id}`) | |
| Копирование чек-листа (`POST /checklists/copy`) | |
| Добавление задачи в чек-лист (`POST /checklists/{id}/tasks`) | |
| Удаление задачи из чек-листа (`DELETE /checklists/{id}/tasks/{task_id}`) | |
| Валидация обязательных полей (`checklist_name`, `machine_id`, `task_id`) | |
| Валидация внешних ключей (`machine_id`, `task_id`) | |
| Валидация статуса (`true`/`false`) | |
| Бизнес-логика: только один активный чек-лист на станок | |
| Деактивация предыдущего чек-листа при создании нового | |
| Обработка ошибок (`400`, `404`, `409`) | |
| Пагинация (`limit`, `offset`) | |

### Инварианты контракта (проверять в каждом кейсе)

- **Источник истины — HTTP-статус.** В JSON нет полей `response_code` / `error_code`.
- **Тело ответа:** успех с данными → `200` + массив объектов; создание → `201` + объект; удаление → `204` без тела.
- **Пагинация:** параметры `limit` (дефолт 0, макс 500) и `offset` (дефолт 0).

## 3. Endpoint-ы — Checklists

| ID | Метод | Endpoint | Назначение | Приоритет | Статус |
| --- | --- | --- | --- | --- | --- |
| API-01 | `GET` | `/api/v1/checklists` | Получить список чек-листов | `High` | `Pass` |
| API-02 | `GET` | `/api/v1/checklists?limit=&offset=` | Получить список с пагинацией | `High` | `Pass` |
| API-03 | `GET` | `/api/v1/checklists?checklist_id=` | Получить чек-лист по ID | `High` | `Pass` |
| API-04 | `GET` | `/api/v1/checklists?machine_id=` | Получить чек-листы по станку | `High` | `Pass` |
| API-05 | `GET` | `/api/v1/checklists?status=` | Получить чек-листы по статусу | `High` | `Pass` |
| API-06 | `POST` | `/api/v1/checklists` | Создать чек-лист | `High` | `Pass` |
| API-07 | `POST` | `/api/v1/checklists/copy` | Копировать чек-лист | `High` | `Pass` |
| API-08 | `POST` | `/api/v1/checklists/{id}/tasks` | Добавить задачу в чек-лист | `High` | `Pass` |
| API-09 | `PATCH` | `/api/v1/checklists/{id}` | Обновить чек-лист (частично) | `Medium` | `Pass` |
| API-10 | `DELETE` | `/api/v1/checklists/{id}` | Удалить чек-лист | `High` | `Pass` |
| API-11 | `DELETE` | `/api/v1/checklists/{id}/tasks/{task_id}` | Удалить задачу из чек-листа | `High` | `Pass` |

## 4. Предусловия и данные — Checklists

### Предусловия

- Окружение доступно: `GET /ping` → `200`.
- В БД существует хотя бы один чек-лист (получается динамически через API).
- В БД существуют задачи для выбора новых значений.
- Для тестов создаются тестовые станки и зона (автоматически очищаются).
- Для тестов используются реальные данные, полученные из системы.

### Тестовые данные — Checklists

| Название | Значение | Где используется | Примечание |
|----------|----------|------------------|------------|
| Checklist ID (существующий) | Динамически из API | TC-003, TC-016, TC-017, TC-018, TC-019, TC-020, TC-021, TC-022, TC-024 | Получается через GET /checklists |
| Checklist Name (существующий) | Динамически из API | TC-003, TC-016 | Получается из первого чек-листа |
| Machine ID (существующий) | Динамически из API | TC-003, TC-005, TC-021 | Получается из первого чек-листа |
| Task IDs (существующие) | Динамически из API | TC-003, TC-008, TC-009, TC-014, TC-018, TC-019, TC-020, TC-026 | Получается из первого чек-листа |
| Status (существующий) | Динамически из API | TC-003, TC-017 | Получается из первого чек-листа |
| New Task ID | Динамически из API | TC-011, TC-012, TC-013, TC-018, TC-025, TC-026, TC-027, TC-028, TC-029, TC-030 | Выбирается из задач, не привязанных к чек-листу |
| Area ID | Динамически из API | TC-008, TC-009, TC-010, TC-011, TC-012, TC-013, TC-014, TC-022, TC-025, TC-026, TC-027, TC-028, TC-029, TC-030 | Создается для тестовых станков |
| Created Machine ID 1 | Динамически из API | TC-008, TC-010, TC-011, TC-012, TC-013, TC-014, TC-022, TC-025, TC-026, TC-027, TC-028, TC-029, TC-030 | Создается в setup |
| Created Machine ID 2 | Динамически из API | TC-008, TC-009, TC-010, TC-011, TC-012, TC-013, TC-014, TC-022, TC-025, TC-026, TC-027, TC-028, TC-029, TC-030 | Создается в setup |
| Checklist ID (несуществующий) | `9999999` | TC-004, TC-015, TC-023 | Не существует в БД |
| Machine ID (несуществующий) | `9999999` | TC-006, TC-009, TC-021, TC-024 | Не существует в БД |
| Task ID (несуществующий) | `9999999` | TC-010, TC-020, TC-028, TC-030 | Не существует в БД |
| Name (для создания) | `Test Checklist` | TC-008, TC-012, TC-013, TC-014, TC-016, TC-025, TC-026, TC-027, TC-028, TC-029, TC-030 | Название для создания/обновления |
| Limit | `10` | TC-002 | Лимит для пагинации |
| Offset | `1` | TC-002 | Смещение для пагинации |

## 5. Тест-кейсы — Checklists

| ID | Endpoint | Сценарий | Тип | Шаги / запрос | Ожидаемый результат | Фактический результат | Статус | Доказательства |
|-----|----------|----------|-----|---------------|----------------------|-----------------------|--------|----------------|
| TC-001 | `GET /checklists` | Получение списка чек-листов | `Positive` | GET без параметров | `200` + массив объектов; структура содержит `checklist_id`, `checklist_name`, `machine_id`, `status`, `task_id` | Совпадает с ожидаемым | `Pass` | |
| TC-002 | `GET /checklists?limit=&offset=` | Получение списка с пагинацией | `Positive` | GET с параметрами `limit=10&offset=1` | `200` + массив объектов; чек-лист с ID из test_data исключен | Совпадает с ожидаемым | `Pass` | |
| TC-003 | `GET /checklists?checklist_id=` | Получение чек-листа по ID | `Positive` | GET с `checklist_id` из test_data | `200` + массив с одним объектом; данные соответствуют | Совпадает с ожидаемым | `Pass` | |
| TC-004 | `GET /checklists?checklist_id=` | Получение по несуществующему ID | `Negative` | GET с `checklist_id: 9999999` | `200` + пустой массив `[]` | Совпадает с ожидаемым | `Pass` | |
| TC-005 | `GET /checklists?machine_id=` | Получение чек-листов по machine_id | `Positive` | GET с `machine_id` из test_data | `200` + массив объектов; у всех `machine_id` равен переданному | Совпадает с ожидаемым | `Pass` | |
| TC-006 | `GET /checklists?machine_id=` | Получение по несуществующему machine_id | `Negative` | GET с `machine_id: 9999999` | `200` + пустой массив `[]` | Совпадает с ожидаемым | `Pass` | |
| TC-007 | `GET /checklists?status=` | Получение чек-листов по статусу | `Positive` | **Параметризованный тест:** `true`, `false` | `200` + массив объектов; у всех `status` равен переданному | Совпадает с ожидаемым | `Pass` | |
| TC-008 | `POST /checklists` | Создание чек-листа с валидными данными | `Positive` | POST с телом: <br/> `{"checklist_name": "Test Checklist", "machine_id": <machine_id>, "task_id": [<task_ids>]}` | `201` + созданный объект; поля соответствуют; статус `true` | Совпадает с ожидаемым | `Pass` | |
| TC-009 | `POST /checklists` | Создание с несуществующим machine_id | `Negative` | POST с `machine_id: 9999999` | Должен быть `409` + ошибка валидации | Совпадает с ожидаемым | `Pass` | |
| TC-010 | `POST /checklists` | Создание с несуществующим task_id | `Negative` | POST с `task_id: [9999999]` | Должен быть `409` + ошибка валидации | Совпадает с ожидаемым | `Pass` | |
| TC-011 | `POST /checklists` | Создание с дублирующимся task_id | `Negative` | POST с `task_id: [id, id]` | Должен быть `400` + ошибка валидации | Совпадает с ожидаемым | `Pass` | |
| TC-012 | `POST /checklists` | Создание на занятом станке | `Negative` | POST на станок с активным чек-листом | Должен быть `409` + ошибка `machine is busy` | Совпадает с ожидаемым | `Pass` | |
| TC-013 | `POST /checklists` | Создание после деактивации предыдущего | `Positive` | 1. Создать чек-лист <br/> 2. Деактивировать его <br/> 3. Создать новый на том же станке | `201` + новый чек-лист создан; старый деактивирован | Совпадает с ожидаемым | `Pass` | |
| TC-014 | `DELETE /checklists/{id}` | Удаление существующего чек-листа | `Positive` | 1. Создать чек-лист <br/> 2. DELETE по ID | `204 No Content` | Совпадает с ожидаемым | `Pass` | |
| TC-015 | `DELETE /checklists/{id}` | Удаление по несуществующему ID | `Negative` | DELETE `id: 9999999` | `404` + ошибка `checklist not found` | Совпадает с ожидаемым | `Pass` | |
| TC-016 | `PATCH /checklists/{id}` | Обновление названия чек-листа | `Positive` | 1. Взять существующий чек-лист <br/> 2. PATCH с новым именем <br/> 3. Восстановить исходное имя | `200` + обновлённый объект; `checklist_name` изменено | Совпадает с ожидаемым | `Pass` | |
| TC-017 | `PATCH /checklists/{id}` | Обновление статуса чек-листа | `Positive` | 1. Взять существующий чек-лист <br/> 2. PATCH с новым статусом <br/> 3. Восстановить исходный статус | `200` + обновлённый объект; `status` изменен | Совпадает с ожидаемым | `Pass` | |
| TC-018 | `PATCH /checklists/{id}` | Добавление задачи в чек-лист | `Positive` | 1. Взять существующий чек-лист <br/> 2. PATCH с добавлением новой задачи <br/> 3. Восстановить исходные задачи | `200` + обновлённый объект; задача добавлена в `task_id` | Совпадает с ожидаемым | `Pass` | |
| TC-019 | `PATCH /checklists/{id}` | Обновление с дублирующейся задачей | `Negative` | PATCH с `task_id` содержащим дубликат | Должен быть `400` + ошибка валидации | Совпадает с ожидаемым | `Pass` | |
| TC-020 | `PATCH /checklists/{id}` | Обновление с несуществующей задачей | `Negative` | PATCH с `task_id: [9999999]` | Должен быть `409` + ошибка валидации | Совпадает с ожидаемым | `Pass` | |
| TC-021 | `PATCH /checklists/{id}` | Обновление с несуществующим станком | `Negative` | PATCH с `machine_id: 9999999` | Должен быть `409` + ошибка валидации | Совпадает с ожидаемым | `Pass` | |
| TC-022 | `POST /checklists/copy` | Копирование чек-листа на другой станок | `Positive` | POST с телом: <br/> `{"source_checklist_id": <id>, "target_machine_id": <machine_id>}` | `201` + скопированный чек-лист; статус `true` | Совпадает с ожидаемым | `Pass` | |
| TC-023 | `POST /checklists/copy` | Копирование несуществующего чек-листа | `Negative` | POST с `source_checklist_id: 9999999` | `404` + ошибка `not found` | Совпадает с ожидаемым | `Pass` | |
| TC-024 | `POST /checklists/copy` | Копирование на несуществующий станок | `Negative` | POST с `target_machine_id: 9999999` | Должен быть `409` + ошибка валидации | Совпадает с ожидаемым | `Pass` | |
| TC-025 | `POST /checklists/copy` | Копирование на станок с активным чек-листом | `Positive` | 1. Создать активный чек-лист на станке <br/> 2. Скопировать другой чек-лист на этот станок | `201` + чек-лист скопирован; старый чек-лист деактивирован | Совпадает с ожидаемым | `Pass` | |
| TC-026 | `POST /checklists/{id}/tasks` | Добавление задачи в чек-лист | `Positive` | 1. Создать чек-лист <br/> 2. POST с `task_id` | `201` + обновлённый чек-лист; задача добавлена | Совпадает с ожидаемым | `Pass` | |
| TC-027 | `POST /checklists/{id}/tasks` | Добавление дублирующейся задачи | `Negative` | 1. Создать чек-лист с задачей <br/> 2. POST с той же `task_id` | Должен быть `409` + ошибка `task already exists` | Совпадает с ожидаемым | `Pass` | |
| TC-028 | `POST /checklists/{id}/tasks` | Добавление несуществующей задачи | `Negative` | POST с `task_id: 9999999` | Должен быть `409` + ошибка валидации | Совпадает с ожидаемым | `Pass` | |
| TC-029 | `DELETE /checklists/{id}/tasks/{task_id}` | Удаление задачи из чек-листа | `Positive` | 1. Создать чек-лист с задачей <br/> 2. DELETE по `task_id` | `204 No Content`; задача удалена из чек-листа | Совпадает с ожидаемым | `Pass` | |
| TC-030 | `DELETE /checklists/{id}/tasks/{task_id}` | Удаление несуществующей задачи | `Negative` | DELETE с `task_id: 9999999` | `404` + ошибка `not found` | Совпадает с ожидаемым | `Pass` | |

## 6. Дефекты — Checklists

| ID | Краткое описание | Severity | Endpoint / метод | Шаги воспроизведения | Ожидаемо | Фактически | X-Request-Id | Статус |
|-----|------------------|----------|------------------|----------------------|----------|------------|--------------|--------|
| BUG-001 | Нет выявленных дефектов | - | - | - | - | - | - | - |

## 7. Итог — Checklists

| Статус | Количество |
|--------|------------|
| Pass | 30 |
| Fail | 0 |
