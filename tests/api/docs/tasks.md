## 1. Паспорт проверки — Tasks

| Поле | Значение |
| --- | --- |
| Проект | pandoraChecklists |
| Функциональность | `tasks` (задачи) |
| Окружение | `Dev / Test / Staging` |
| Версия API / Commit | _тег/commit_ |
| Base URL | `http://localhost:8081` |
| Документация | Swagger `/swagger/index.html` |
| Роль / способ авторизации | Токенов нет. Внутренняя сеть |
| Проба живости | `GET /ping` → `200`, пустое тело |
| Итог | `Pass / Fail / Blocked / Not Run` |

## 2. Объём проверки — Tasks

| Что проверяется | Что не проверяется / ограничения |
| --- | -- |
| Создание задачи (`POST /tasks`) | |
| Получение списка задач (`GET /tasks`) | |
| Получение задачи по ID (`GET /tasks?task_id=`) | |
| Обновление задачи (`PATCH /tasks/{id}`) | |
| Удаление задачи (`DELETE /tasks/{id}`) | |
| Валидация обязательных полей (`task_name`) | |
| Валидация уникальности поля `task_name` | |
| Обработка пустых значений при создании | |
| Обработка ошибок (`400`, `404`, `409`) | |
| Пагинация (`limit`, `offset`) | |

### Инварианты контракта (проверять в каждом кейсе)

- **Источник истины — HTTP-статус.** В JSON нет полей `response_code` / `error_code`.
- **Тело ответа:** успех с данными → `200` + массив объектов; создание → `201` + объект; удаление → `204` без тела.
- **Пагинация:** параметры `limit` (дефолт 0, макс 500) и `offset` (дефолт 0).

## 3. Endpoint-ы — Tasks

| ID | Метод | Endpoint | Назначение | Приоритет | Статус |
| --- | --- | --- | --- | --- | --- |
| API-01 | `GET` | `/api/v1/tasks` | Получить список задач | `High` | `Pass` |
| API-02 | `GET` | `/api/v1/tasks?limit=&offset=` | Получить список с пагинацией | `High` | `Pass` |
| API-03 | `GET` | `/api/v1/tasks?task_id=` | Получить задачу по ID | `High` | `Pass` |
| API-04 | `POST` | `/api/v1/tasks` | Создать задачу | `High` | `Pass` |
| API-05 | `PATCH` | `/api/v1/tasks/{id}` | Обновить задачу (частично) | `Medium` | `Pass` |
| API-06 | `DELETE` | `/api/v1/tasks/{id}` | Удалить задачу | `High` | `Pass` |

## 4. Предусловия и данные — Tasks

### Предусловия

- Окружение доступно: `GET /ping` → `200`.
- В БД существует хотя бы одна задача (получается динамически через API).
- Для тестов используется реальная задача, полученная из системы.

### Тестовые данные — Tasks

| Название | Значение | Где используется | Примечание |
|----------|----------|------------------|------------|
| Task ID (существующий) | Динамически из API | TC-003, TC-010 | Получается через GET /tasks |
| Task Name (существующий) | Динамически из API | TC-006, TC-011 | Получается из первой задачи |
| Task ID (несуществующий) | `9999999` | TC-004, TC-009 | Не существует в БД |
| Name (для создания) | `test task` | TC-005, TC-008, TC-011 | Название для создания/обновления |
| Limit | `10` | TC-002 | Лимит для пагинации |
| Offset | `1` | TC-002 | Смещение для пагинации |

## 5. Тест-кейсы — Tasks

| ID | Endpoint | Сценарий | Тип | Шаги / запрос | Ожидаемый результат | Фактический результат | Статус | Доказательства |
|-----|----------|----------|-----|---------------|----------------------|-----------------------|--------|----------------|
| TC-001 | `GET /tasks` | Получение списка задач | `Positive` | GET без параметров | `200` + массив объектов; структура объектов содержит поля `task_id` и `task_name` | Совпадает с ожидаемым | `Pass` | |
| TC-002 | `GET /tasks?limit=&offset=` | Получение списка задач с пагинацией | `Positive` | GET с параметрами `limit=10&offset=1` | `200` + массив объектов; количество объектов ≤ `limit`; задача с ID из test_data исключена из результатов (offset=1) | Совпадает с ожидаемым | `Pass` | |
| TC-003 | `GET /tasks?task_id=` | Получение задачи по существующему ID | `Positive` | GET с `task_id` из test_data | `200` + массив с одним объектом; `task_id` и `task_name` соответствуют данным из системы | Совпадает с ожидаемым | `Pass` | |
| TC-004 | `GET /tasks?task_id=` | Получение задачи по несуществующему ID | `Negative` | GET с `task_id: 9999999` | `200` + пустой массив `[]` | Совпадает с ожидаемым | `Pass` | |
| TC-005 | `POST /tasks` | Создание задачи с валидными данными | `Positive` | POST с телом: <br/> `{"task_name": "test task"}` | `201` + созданный объект; поле `task_name` соответствует переданному; есть `task_id` | Совпадает с ожидаемым | `Pass` | |
| TC-006 | `POST /tasks` | Создание задачи с дублирующимся `task_name` | `Negative` | POST с телом: <br/> `{"task_name": "<имя_из_существующей_задачи>"}` | Должен быть `409` + ошибка `resource already exists` | **БАГ:** API возвращает `204` (No Content) вместо `409` | `Fail` | |
| TC-007 | `POST /tasks` | Создание задачи с пустым `task_name` | `Negative` | POST с телом: <br/> `{"task_name": ""}` | Должен быть `400` + ошибка валидации | Совпадает с ожидаемым | `Pass` | |
| TC-008 | `DELETE /tasks/{id}` | Удаление существующей задачи | `Positive` | 1. Создать задачу <br/> 2. DELETE по ID созданной задачи | `204 No Content` | Совпадает с ожидаемым | `Pass` | |
| TC-009 | `DELETE /tasks/{id}` | Удаление по несуществующему ID | `Negative` | DELETE `id: 9999999` | `404` + ошибка `task not found` | Совпадает с ожидаемым | `Pass` | |
| TC-010 | `PATCH /tasks/{id}` | Обновление задачи | `Positive` | 1. Взять существующую задачу <br/> 2. PATCH с новым именем `test task` <br/> 3. Проверить что имя обновилось <br/> 4. Восстановить исходное имя | `200` + обновлённый объект; поле `task_name` обновлено на новое значение | Совпадает с ожидаемым | `Pass` | |
| TC-011 | `PATCH /tasks/{id}` | Обновление с дублирующимся `task_name` | `Negative` | 1. Создать задачу <br/> 2. PATCH с именем существующей задачи | Должен быть `409` + ошибка `resource already exists` | **БАГ:** API возвращает `200` и обновляет задачу с дублирующимся именем вместо `409` | `Fail` | |

## 6. Дефекты — Tasks

| ID | Краткое описание | Severity | Endpoint / метод | Шаги воспроизведения | Ожидаемо | Фактически | X-Request-Id | Статус |
|-----|------------------|----------|------------------|----------------------|----------|------------|--------------|--------|
| BUG-001 | Отсутствует валидация уникальности `task_name` при создании задачи (некорректный статус ответа) | `Critical` | `POST /tasks` | 1. Создать задачу с уникальным именем <br/> 2. Создать другую задачу с таким же именем | Должна быть ошибка `409` с описанием `resource already exists` | API возвращает `204` (No Content) вместо `409` | | `Open` |
| BUG-002 | Отсутствует валидация уникальности `task_name` при обновлении задачи | `Major` | `PATCH /tasks/{id}` | 1. Создать задачу <br/> 2. Создать другую задачу <br/> 3. Обновить вторую задачу, указав имя первой | Должна быть ошибка `409` с описанием `resource already exists` | API возвращает `200` и обновляет задачу с дублирующимся именем | | `Open` |

## 7. Итог — Tasks

| Статус | Количество |
|--------|------------|
| Pass | 9 |
| Fail | 2 |
